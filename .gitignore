# Your saved login session — treat like a password, never commit.
user_data/

# Captured runs (HTML/screenshots + cookies; contain live session secrets).
captures/
secrets/
session_backup/

# Your personal cover note (used by `wf_apply --note-file`). Keep it private.
note.txt

__pycache__/
*.pyc
.venv/
venv/
.DS_Store
